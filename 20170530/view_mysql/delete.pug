//
   Created by kimilsik on 6/14/17.
doctype html
html
    head
        meta(charset='utf-8')
    body
        h1
            a(href='/topic') Server side javascript
        ul
            each t in topics
                li
                    a(href='/topic/' + t.id)=t.title
        article
            h1= 'Delete ' + top.title + ' ?'
            form(action='/topic/'+ top.id +'/delete', method='post')
                // delete or update는 post로 하는 게 좋다
                p
                    input(type='submit' value='yes')

            a(href='/topic/'+ top.id) no
                //p
                //    input(type='submit' value='no') // no 는 get 방식으로 해도 된다