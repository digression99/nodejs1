//
    lesson 64 - 문서의 기하학적 특성
        -> 위치, 크기, 스크롤 등을 제어하는 방법.

doctype html
html
    head
        meta(charset="utf-8")
        style.
            body {
                padding: 0;
                margin: 0;
            }

            #target {
                width: 100px;
                height: 100px;
                border: 50px solid #1065e6;
                padding: 50px;
                margin: 50px;
            }
            div {
                border: 50px solid #1065e6;
                padding: 50px;
                margin: 50px;
            }
    body
        div
            div(id="target") Coding


        script(src="/jwb/jquery-3.2.1.js")

        script.
            var t = document.getElementById('target');
            console.log(t.getBoundingClientRect());
            // div를 감싸는 직사각형의 내용을 볼 수 있다.
            // 이걸 이용해
            // margin border padding contents 순으로 들어간다.
            console.log(t.offsetParent);
            // body가 기준점이 됨을 알 수 있다. 즉 바디를 기준으로 픽셀 수를 재는 것이다.
            console.log('clientWidth : ', t.clientWidth, 'clientHeight :', t.clientHeight);

