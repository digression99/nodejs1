//
    * command + d -> 해당 줄을 복사 붙여넣기

    lesson 35 - jQuery 객체

     li.css('text-decoration');
     -> 두 번째 인자가 없으면 이 값을 가져옴.
     두 번째 인자가 있다면 설정.
     가져올 때는 가장 먼저 있는 것 하나만 가져온다.
    var li = $('li');
                li.css('text-decoration', 'underline');
                -> li 의 특성을 가진 모든 객체에 underline 적용 ==> 암시적 반복

    https://api.jquery.com
    -> jQuery 의 api 문서.




doctype html
html
    head
        meta(charset="utf-8")
    body
        ul
            li(id="target") html
            li css
            li javascript

        script(src="/jwb/jquery-3.2.1.js")

        script.
            var li = $('li');
            // 이것은 jQuery 객체

            li.map(function(index, elem) {
                console.log(index, elem);
                $(elem).css('color', 'red');
            })

            // li의 객체에서 map으로 함수를 전달.
            // map으로 전달된 함수는 li 안에 있는 모든 element에 대해서 함수를 적용..

            // li[0] -> DOM 객체
            // -> li[0].css('~') 은 에러.
            //for(var i = 0; i < li.length; ++i)
            //    console.log($(li[i]).css('text-decoration', 'underline');
            // 이렇게 해야 jQuery객체를 이용할 수 있다.

            //var t = document.getElementById('target');
            // t.constructor -> dom object.




























